/**
 * This queue holds the test recording sessions that are waiting to be started
 */
export const RECORDING_SESSIONS_QUEUE = 'RECORDING_SESSIONS_QUEUE';

/**
 * This queue holds the test runs that are waiting to be started
 */
export const TEST_RUN_QUEUE = 'TEST_RUN_QUEUE';

/**
 * The run process is the handle for the method that will run
 */
export const TEST_RECORDER_PROCESS = 'TEST_RECORDER_PROCESS';

/**
 * The run process is the handle for the method that will run
 */
export const TEST_RUN_PROCESS = 'TEST_RUN_PROCESS';

/**
 * This queue holds the test recording sessions that are waiting to be started
 */
export const CONTAINER_TERMINATION_QUEUE = 'CONTAINER_TERMINATION_QUEUE';
